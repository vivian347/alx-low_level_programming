#include <stdio.h>

/**
 * main - prints program name;
 * @argc: countof arguments
 * @argv: array of pointers tothe argument
 *
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
